import React from "react"

const GooglePage = () => {
  return (
    <div>
      <p>
        Google relies on massive amounts of data collection. Whether it is through
        Android phones, Google Analytics or search, Google uses various
        pipelines to gain data that it can use to sell advertisements.
      </p>
    </div>
  )
}

export default GooglePage;